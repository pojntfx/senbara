#pragma once

#if !defined(SENBARA_GTK_INSIDE) && !defined(SENBARA_GTK_COMPILATION)
# error "Only <senbara-gtk.h> can be included directly."
#endif

/**
 * SECTION:senbara-gtkversion
 * @short_description: senbara-gtk version checking
 *
 * senbara-gtk provides macros to check the version of the library
 * at compile-time
 */

/**
 * SENBARA_GTK_MAJOR_VERSION:
 *
 * senbara-gtk major version component (e.g. 1 if %SENBARA_GTK_VERSION is 1.2.3)
 */
#define SENBARA_GTK_MAJOR_VERSION (@MAJOR_VERSION@)

/**
 * SENBARA_GTK_MINOR_VERSION:
 *
 * senbara-gtk minor version component (e.g. 2 if %SENBARA_GTK_VERSION is 1.2.3)
 */
#define SENBARA_GTK_MINOR_VERSION (@MINOR_VERSION@)

/**
 * SENBARA_GTK_MICRO_VERSION:
 *
 * senbara-gtk micro version component (e.g. 3 if %SENBARA_GTK_VERSION is 1.2.3)
 */
#define SENBARA_GTK_MICRO_VERSION (@MICRO_VERSION@)

/**
 * SENBARA_GTK_VERSION
 *
 * senbara-gtk version.
 */
#define SENBARA_GTK_VERSION (@VERSION@)

/**
 * SENBARA_GTK_VERSION_S:
 *
 * senbara-gtk version, encoded as a string, useful for printing and
 * concatenation.
 */
#define SENBARA_GTK_VERSION_S "@VERSION@"

#define SENBARA_GTK_ENCODE_VERSION(major,minor,micro) \
        ((major) << 24 | (minor) << 16 | (micro) << 8)

/**
 * SENBARA_GTK_VERSION_HEX:
 *
 * senbara-gtk version, encoded as an hexadecimal number, useful for
 * integer comparisons.
 */
#define SENBARA_GTK_VERSION_HEX \
        (SENBARA_GTK_ENCODE_VERSION (SENBARA_GTK_MAJOR_VERSION, SENBARA_GTK_MINOR_VERSION, SENBARA_GTK_MICRO_VERSION))

/**
 * SENBARA_GTK_CHECK_VERSION:
 * @major: required major version
 * @minor: required minor version
 * @micro: required micro version
 *
 * Compile-time version checking. Evaluates to %TRUE if the version
 * of senbara-gtk is greater than the required one.
 */
#define SENBARA_GTK_CHECK_VERSION(major,minor,micro)   \
        (SENBARA_GTK_MAJOR_VERSION > (major) || \
         (SENBARA_GTK_MAJOR_VERSION == (major) && SENBARA_GTK_MINOR_VERSION > (minor)) || \
         (SENBARA_GTK_MAJOR_VERSION == (major) && SENBARA_GTK_MINOR_VERSION == (minor) && \
          SENBARA_GTK_MICRO_VERSION >= (micro)))
